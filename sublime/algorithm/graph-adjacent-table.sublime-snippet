<snippet>
    <content><![CDATA[
const adj = {}
edges.forEach(([u, v]) => {
    adj[u] = adj[u] || []
    adj[v] = adj[v] || []
    adj[u].push(v)
    adj[v].push(u)
})
]]></content>
    <tabTrigger>adj</tabTrigger>
    <scope>source.js</scope>
    <description>Graph - Adjacent Table</description>
</snippet>
